#!/bin/bash -e
# regenerate redmine secrets

cat > /var/www/railsapp/config/additional_environment.rb <<EOF
config.action_controller.session = { :key => "_myapp_session", :secret => "$(mcookie)" }
EOF

